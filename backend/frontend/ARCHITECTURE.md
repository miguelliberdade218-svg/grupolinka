# 🏗️ DIAGRAMA DA ARQUITETURA - Link-A Frontend

## 📊 VISÃO GERAL DO FLUXO

```
┌─────────────────────────────────────────────────────────────────┐
│                     APLICAÇÃO FRONTEND (VITE)                   │
│                     src/apps/hotels-app/                        │
└─────────────────────────────────────────────────────────────────┘

         ┌──────────────────────────────────────────┐
         │     HotelManagerDashboard.tsx            │
         │  (Página principal do gerenciador)       │
         │                                          │
         │  - Estatísticas em tempo real             │
         │  - Abas: Overview, Quartos, Eventos      │
         │  - Integração com todos os componentes   │
         └──────────────────────────────────────────┘
                    │
         ┌──────────┴──────────────────────────┐
         │                                      │
    ┌────▼─────────────────┐    ┌──────▼──────────────────┐
    │ RoomTypesManagement  │    │ EventSpacesManagement   │
    │ (src/components/     │    │ (src/components/        │
    │  room-types/)        │    │  event-spaces/)         │
    │                      │    │                         │
    │ - Lista quartos      │    │ - Lista espaços         │
    │ - Create/Edit/Delete │    │ - Create/Edit/Delete    │
    │ - Amenidades         │    │ - Features              │
    │ - Preços             │    │ - Preços                │
    └──────────┬───────────┘    └─────────┬───────────────┘
               │                          │
               └──────────────┬───────────┘
                              │
           ┌──────────────────▼────────────────────┐
           │     SERVIÇOS (Services Layer)         │
           ├──────────────────────────────────────┤
           │                                      │
           │  ┌────────────────────────────────┐  │
           │  │   hotelService.ts              │  │
           │  │                                │  │
           │  │  Métodos:                      │  │
           │  │  ✅ searchHotels()             │  │
           │  │  ✅ getHotelById()             │  │
           │  │  ✅ createHotel()              │  │
           │  │  ✅ getRoomTypesByHotel()      │  │
           │  │  ✅ createRoomType()           │  │
           │  │  ✅ updateRoomType()           │  │
           │  │  ✅ deleteRoomType()           │  │
           │  │  ✅ getBookingsByHotel()       │  │
           │  │  ✅ createBooking()            │  │
           │  │  ✅ checkInBooking()           │  │
           │  │  ✅ checkOutBooking()          │  │
           │  │  ✅ cancelBooking()            │  │
           │  │  ✅ getPromotionsByHotel()     │  │
           │  │  ✅ createPromotion()          │  │
           │  │  ✅ getReviewsByHotel()        │  │
           │  │  ... (30+ total)               │  │
           │  └────────────────────────────────┘  │
           │                                      │
           │  ┌────────────────────────────────┐  │
           │  │   eventSpaceService.ts         │  │
           │  │                                │  │
           │  │  Métodos:                      │  │
           │  │  ✅ searchEventSpaces()        │  │
           │  │  ✅ getEventSpaceById()        │  │
           │  │  ✅ createEventSpace()         │  │
           │  │  ✅ getEventSpacesByHotel()    │  │
           │  │  ✅ createEventBooking()       │  │
           │  │  ✅ getEventBookingsBySpace()  │  │
           │  │  ✅ confirmEventBooking()      │  │
           │  │  ✅ rejectEventBooking()       │  │
           │  │  ✅ cancelEventBooking()       │  │
           │  │  ✅ getEventSpaceReviews()     │  │
           │  │  ... (30+ total)               │  │
           │  └────────────────────────────────┘  │
           │                                      │
           └──────────────────┬───────────────────┘
                              │
           ┌──────────────────▼────────────────────┐
           │      HTTP CLIENT (api.ts)             │
           │                                      │
           │  - Gerencia requisições HTTP          │
           │  - Tratamento de erros               │
           │  - Autenticação (Bearer token)        │
           │  - CORS handling                     │
           └──────────────────┬───────────────────┘
                              │
           ┌──────────────────▼────────────────────┐
           │     BACKEND REST API                 │
           │  http://localhost:8000               │
           │  /api/v2/                            │
           ├──────────────────────────────────────┤
           │                                      │
           │  ┌────────────────────────────────┐  │
           │  │   HOTÉIS CONTROLLER            │  │
           │  │  src/modules/hotels/           │  │
           │  │                                │  │
           │  │  Endpoints:                    │  │
           │  │  GET    /hotels                │  │
           │  │  GET    /hotels/:id            │  │
           │  │  POST   /hotels                │  │
           │  │  PUT    /hotels/:id            │  │
           │  │  GET    /hotels/:id/room-types │  │
           │  │  POST   /hotels/:id/room-types │  │
           │  │  PUT    /hotels/:id/room-types │  │
           │  │  DELETE /hotels/:id/room-types │  │
           │  │  GET    /hotels/:id/bookings   │  │
           │  │  POST   /hotels/:id/bookings   │  │
           │  │  POST   /bookings/:id/check-in │  │
           │  │  POST   /bookings/:id/check-out│  │
           │  │  ... (e mais)                  │  │
           │  └────────────────────────────────┘  │
           │                                      │
           │  ┌────────────────────────────────┐  │
           │  │   EVENTOS CONTROLLER           │  │
           │  │  src/modules/events/           │  │
           │  │                                │  │
           │  │  Endpoints:                    │  │
           │  │  GET    /events/spaces         │  │
           │  │  GET    /events/spaces/:id     │  │
           │  │  POST   /events/spaces         │  │
           │  │  PUT    /events/spaces/:id     │  │
           │  │  DELETE /events/spaces/:id     │  │
           │  │  GET    /events/hotel/:id      │  │
           │  │  POST   /events/spaces/bookings│  │
           │  │  GET    /events/bookings/:id   │  │
           │  │  POST   /events/bookings/conf  │  │
           │  │  POST   /events/bookings/rej   │  │
           │  │  ... (e mais)                  │  │
           │  └────────────────────────────────┘  │
           │                                      │
           │  ┌────────────────────────────────┐  │
           │  │   DATABASE (PostgreSQL)        │  │
           │  │                                │  │
           │  │  Tabelas:                      │  │
           │  │  - hotels                      │  │
           │  │  - room_types                  │  │
           │  │  - hotel_bookings              │  │
           │  │  - event_spaces                │  │
           │  │  - event_bookings              │  │
           │  │  - promotions                  │  │
           │  │  - reviews                     │  │
           │  │  ... (e mais)                  │  │
           │  └────────────────────────────────┘  │
           │                                      │
           └──────────────────────────────────────┘
```

---

## 🔄 FLUXO DE DADOS - EXEMPLO

### Cenário: Listar Room Types de um Hotel

```
┌─────────────────────────────────────────────────────────────────────┐
│ 1. COMPONENTE - RoomTypesManagement.tsx                             │
│    ├─ useEffect(() => loadRoomTypes())                             │
│    └─ Chama: hotelService.getRoomTypesByHotel('hotel-uuid-123')     │
└────────────────────────────┬────────────────────────────────────────┘

┌────────────────────────────▼────────────────────────────────────────┐
│ 2. SERVIÇO - hotelService.ts                                       │
│    ├─ async getRoomTypesByHotel(hotelId)                            │
│    ├─ Constrói URL: /api/v2/hotels/:id/room-types                  │
│    └─ Chama: apiService.get<ListResponse<RoomType>>(url)           │
└────────────────────────────┬────────────────────────────────────────┘

┌────────────────────────────▼────────────────────────────────────────┐
│ 3. HTTP CLIENT - api.ts                                            │
│    ├─ get() method                                                  │
│    ├─ Adiciona: Headers, Auth token                                 │
│    ├─ Faz: fetch() request                                          │
│    └─ Retorna: Promise<Response>                                    │
└────────────────────────────┬────────────────────────────────────────┘

┌────────────────────────────▼────────────────────────────────────────┐
│ 4. BACKEND API - hotelController.ts                                │
│    ├─ GET /api/v2/hotels/:id/room-types                            │
│    ├─ Verifica: Autenticação                                        │
│    ├─ Query: SELECT * FROM room_types WHERE hotel_id = ?           │
│    └─ Retorna: { success: true, data: [...], count: 5 }            │
└────────────────────────────┬────────────────────────────────────────┘

┌────────────────────────────▼────────────────────────────────────────┐
│ 5. SERVIÇO - hotelService.ts                                       │
│    ├─ Recebe response JSON                                         │
│    └─ Retorna: { success: true, data: [...], count: 5 }            │
└────────────────────────────┬────────────────────────────────────────┘

┌────────────────────────────▼────────────────────────────────────────┐
│ 6. COMPONENTE - RoomTypesManagement.tsx                             │
│    ├─ setRoomTypes(response.data)                                   │
│    ├─ Re-render com lista atualizada                                │
│    └─ Mostra: Grid com cards de quartos                             │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📁 ESTRUTURA COMPLETA

```
frontend/
├── src/
│   ├── apps/
│   │   ├── main-app/                    ← App para usuários
│   │   │   ├── pages/
│   │   │   ├── components/
│   │   │   └── App.tsx
│   │   │
│   │   ├── drivers-app/                 ← App para drivers ✅ MANTER
│   │   │   ├── pages/
│   │   │   ├── components/
│   │   │   └── App.tsx
│   │   │
│   │   ├── hotels-app/ ✅ CENTRALIZADO
│   │   │   ├── pages/
│   │   │   │   └── hotel-management/
│   │   │   │       └── HotelManagerDashboard.tsx ✅
│   │   │   │
│   │   │   ├── components/
│   │   │   │   ├── room-types/
│   │   │   │   │   └── RoomTypesManagement.tsx ✅
│   │   │   │   ├── event-spaces/
│   │   │   │   │   └── EventSpacesManagement.tsx ✅
│   │   │   │   ├── bookings/           ⏳ PRÓXIMO
│   │   │   │   ├── promotions/         ⏳ PRÓXIMO
│   │   │   │   └── reviews/            ⏳ PRÓXIMO
│   │   │   │
│   │   │   ├── App.tsx
│   │   │   └── routes.tsx
│   │   │
│   │   └── admin-app/                   ← Admin only
│   │       ├── pages/
│   │       ├── components/
│   │       └── App.tsx
│   │
│   ├── services/ ✅ NOVO
│   │   ├── hotelService.ts             ✅ 500 linhas
│   │   ├── eventSpaceService.ts        ✅ 500 linhas
│   │   ├── api.ts
│   │   ├── roomTypeService.ts
│   │   └── locationsService.ts
│   │
│   └── shared/
│       ├── components/
│       ├── admin/
│       ├── hooks/
│       └── utils/
│
├── QUICK_START.md                      ✅ Ler primeiro
├── EXECUTIVE_SUMMARY.md                ✅ Visão geral
├── RESTRUCTURING_PLAN.md               ✅ Detalhes
├── SERVICE_USAGE_GUIDE.md              ✅ 50+ exemplos
├── MIGRATION_CHECKLIST.md              ✅ Progresso
└── ARCHITECTURE.md                     ✅ Este ficheiro
```

---

## 🔗 MAPEAMENTO DE RESPONSABILIDADES

```
┌─────────────────────────────────────────────────────────────┐
│                    RESPONSABILIDADES                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  COMPONENTES (UI Layer)                                    │
│  └─ Renderizar interface                                   │
│  └─ Gerenciar estado local                                 │
│  └─ Chamar serviços                                        │
│  └─ Tratamento de loading/erro                             │
│  └─ Responder a eventos do usuário                         │
│                                                             │
│  SERVIÇOS (Business Logic Layer)                           │
│  └─ Organizar chamadas API                                 │
│  └─ Transformar dados                                      │
│  └─ Validações básicas                                     │
│  └─ Tratamento de erros                                    │
│  └─ Reutilização de lógica                                 │
│                                                             │
│  API CLIENT (Network Layer)                                │
│  └─ Gerenciar requisições HTTP                             │
│  └─ Adicionar headers/auth                                 │
│  └─ CORS handling                                          │
│  └─ Retry logic                                            │
│  └─ Logging                                                │
│                                                             │
│  BACKEND (Server Layer)                                    │
│  └─ Validar entrada                                        │
│  └─ Autenticação                                           │
│  └─ Lógica de negócio                                      │
│  └─ Acesso a dados                                         │
│  └─ Resposta formatada                                     │
│                                                             │
│  DATABASE (Data Layer)                                     │
│  └─ Persistência de dados                                  │
│  └─ Índices/Queries                                        │
│  └─ Backup/Restore                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 FLUXO DE ESTADO

```
1. Componente inicia com: { loading: false, roomTypes: [], error: null }

2. useEffect() → loadRoomTypes()
   └─ setLoading(true)

3. await hotelService.getRoomTypesByHotel('id')
   └─ Chamada HTTP

4. Response chega:
   └─ Se success → setRoomTypes(data)
   └─ Se erro → setError(error)
   └─ setLoading(false)

5. Re-render automático
   └─ Loading? Mostra spinner
   └─ Erro? Mostra mensagem
   └─ Dados? Mostra grid de quartos
```

---

## 🎯 PADRÃO DE ERRO

```
try {
  const response = await hotelService.getHotelById(id);
  
  if (response.success) {
    setHotel(response.data);
  } else {
    setError(response.error);
  }
} catch (error) {
  setError('Erro de conexão');
}
```

---

## ✨ PRÓXIMA EVOLUÇÃO (FASE 2)

```
┌────────────────────────────────────────┐
│   FORMULÁRIOS CRUD (Forms)             │
│                                        │
│   RoomTypeForm.tsx                     │
│   ├─ Criar novo                        │
│   ├─ Editar existente                  │
│   ├─ Delete                            │
│   ├─ Validações Zod                    │
│   └─ Upload imagens                    │
│                                        │
│   EventSpaceForm.tsx                   │
│   ├─ Criar novo                        │
│   ├─ Editar existente                  │
│   ├─ Multi-image upload                │
│   └─ Features checkboxes               │
└────────────────────────────────────────┘
           ▼
┌────────────────────────────────────────┐
│   CALENDÁRIOS & DISPONIBILIDADE        │
│                                        │
│   react-big-calendar integrado         │
│   ├─ Visualizar datas                  │
│   ├─ Bloquear datas                    │
│   ├─ Preços dinâmicos                  │
│   └─ Sincronizar com OTAs              │
└────────────────────────────────────────┘
```

---

**Criado**: 18 Janeiro 2026  
**Versão**: 1.0 - Arquitetura Completa
